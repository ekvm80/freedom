# HTML 변환 작업 계획서 — AI 연구·행정 인프라 가이드

> **작성:** 2026-08-30 · **작업 담당:** Opus 5
> **원본:** 이 폴더의 6개 마크다운 문서 (01, 02, 03, 03-1, 03-2, 04)
> **산출물:** `index.html` **단일 파일** (외부 의존 최소)

---

## 1. 목적

6개 마크다운 문서를 **하나의 인터랙티브 HTML 가이드**로 통합한다.
핵심 요구는 두 가지: **① 식별이 용이할 것** (지금 내가 봐야 할 내용을 3초 안에 찾음), **② 잘 이해될 것** (텍스트로 반복 설명하던 구조를 시각·인터랙션으로 한 번에 전달).

**독자:** 시스템 소유자 본인(비개발자, 교수) + 같은 구성을 따라 만들려는 초보자.

---

## 2. 원본 문서 현황 (정비 완료 상태)

| 파일 | 내용 | HTML에서의 역할 |
| :--- | :--- | :--- |
| `01_시스템_구성_정리.md` | 정본 — 아키텍처·모델·기기·보안 | "시스템 이해" 섹션 |
| `02_일상_및_학술활동_활용_체계.md` | 기기별 역할·작업 배분 원칙 | "언제 뭘 쓰나" 섹션 |
| `03_시스템_구축_단계별_가이드.md` | 구축 허브 — 공통 원칙·로드맵 | "구축" 섹션 도입부 |
| `03-1_구축가이드_프리덤_공통기반.md` | 전 기기: 앱+구독+MCP+SaaS+모바일 (STEP 1~5) | "구축" 트랙 — 프리덤 |
| `03-2_구축가이드_에르메스_비서레이어.md` | 맥 전용: Hermes+게이트웨이+백업 (STEP 1~7) | "구축" 트랙 — 에르메스 |
| `04_요약_및_실전_활용_가이드.md` | 실전 사례 16건 + 치트시트 17행 | "실전 활용" 섹션 |

마크다운 정비는 완료됨(링크·헤딩 위계·약어·용어 통일). **HTML은 원본 md의 내용을 재구성하되, 사실관계를 바꾸지 않는다.**

### 반드시 보존해야 할 핵심 메시지 5가지
1. **구조:** 전 기기 공통 기반(프리덤) 위에, 맥에만 Hermes 비서 레이어(에르메스)를 얹은 구조다. 별개의 두 시스템이 아니다.
2. **안전 설계:** 본체·노트북에 Hermes가 없는 것은 **의도된 안전장치**다. MCP·SaaS가 전 기기에 있어 능력 차이는 없다.
3. **비용 원리:** 같은 Claude라도 **구독 로그인(프리덤)=정액, API(에르메스)=종량**. Claude·Gemini를 길게 쓸 일은 맥에서라도 프리덤으로 연다. ChatGPT만 구독 인증으로 CLI 사용 가능.
4. **기기별 실제 역할:** 맥=비서 본진, 본체=연구·프로젝트 주력, 노트북=주로 원격 단말, 폰=Orca 모바일(본체 이어받기)+텔레그램(위임).
5. **원격 3경로의 성격 차이:** 텔레그램·슬랙=위임 / Orca 모바일=원격 조작(대화 맥락 유지) / Chrome Remote Desktop=화면 제어.

---

## 3. md → HTML 전환에서 해결할 문제 (개선점)

원본은 마크다운의 한계 때문에 다음 문제를 안고 있다. HTML에서 각각 해소한다.

| # | 원본의 문제 | HTML 해법 |
| :--- | :--- | :--- |
| P1 | **핵심 표가 4~6곳에 반복** (인증 방식 차이, 기기별 역할, 원격 3경로) — 수정 시 전부 손대야 함 | **단일 컴포넌트로 1회 정의**, 각 섹션에서 재사용·앵커 참조 |
| P2 | **ASCII 다이어그램 3종**이 정렬 불안·모바일 깨짐 (기기 배치도, 레이어 구조, 이어받기 흐름) | **인라인 SVG로 재작화** — 기기 카드·연결선·역할 라벨, 반응형 |
| P3 | A/B 구분이 **텍스트로만** 표시되어 스캔이 어려움 | **색상 코딩 배지 시스템** — 문서 전체에서 A/B/공통을 색으로 즉시 식별 |
| P4 | 사례 16건·프롬프트 예시 20여 개가 **긴 세로 스크롤** | 아코디언 + 카테고리 필터 + **프롬프트 복사 버튼** |
| P5 | 점검 체크리스트(03-1: 15항목, 03-2: 21항목)가 **읽기만 가능** | **인터랙티브 체크박스 + localStorage 진행률 저장** + 진행률 바 |
| P6 | 치트시트 17행이 표라서 **원하는 행 찾기 어려움** | 창구별(터미널/텔레그램/B/원격) 필터 칩 + 검색 |
| P7 | 6개 파일에 흩어져 **전체 검색 불가** | 상단 전역 검색 (제목+본문, 실시간 하이라이트) |
| P8 | "어느 쪽을 쓸까" 판단표가 수동적 | **인터랙티브 판단 도우미** — 2~3개 질문 → A/B/창구 추천 |

---

## 4. 정보 구조 (IA) — 6개 문서를 5개 뷰로 재편

문서 번호 순서가 아니라 **사용자의 질문 순서**로 재편한다.

```
🏠 홈 (대시보드)
 ├─ 히어로: 한 줄 정의 + 레이어 구조 SVG (프리덤 위에 에르메스)
 ├─ 기기 배치도 SVG (맥·본체·노트북·폰 + 연결선·역할 라벨)
 ├─ Bento 그리드: 핵심 수치 카드 (MCP 43개 도구 · 기기 3대 · 원격 3경로 · AI 엔진 구성)
 └─ "지금 뭘 하려고?" 빠른 진입 버튼 4개 → 각 섹션

🧭 시스템 이해  (← 01)
 ├─ 두 시스템의 관계 (비교 카드)
 ├─ 인증·비용 구조 (⭐ 핵심 컴포넌트: 모델×인증×비용 표)
 ├─ 모델 라우팅 (Timely/OpenRouter 탭)
 ├─ MCP 3종 (도구 카드 3장, 펼치면 세부 도구)
 ├─ 지식베이스 (위키·Atlas) · SaaS
 └─ 보안 원칙 (경고 스타일 카드)

⚖️ 언제 뭘 쓰나  (← 02)
 ├─ 기기별 실제 역할 (재사용 컴포넌트)
 ├─ 판단 도우미 (인터랙티브, P8)
 ├─ 같은 맥에서 갈아타기 (A↔B 비용 대비 강조)
 └─ 표준 워크플로 (B에서 생각 → A에게 위임) 흐름 SVG

🔧 구축 가이드  (← 03 + 03-1 + 03-2)
 ├─ 시작 전 원칙 (AI에게 묻는 법 · 4요소 · 에러 붙여넣기)
 ├─ 트랙 선택: [B — 공통 기반 ⭐쉬움] [A — Hermes 비서 ⭐⭐⭐보통]
 ├─ 각 트랙: STEP 스텝퍼 UI (진행 단계 시각화, 완료 판정 기준 명시)
 │    └ "AI에게 이렇게 물어보세요" 블록 = 복사 버튼 필수
 └─ 점검 체크리스트 (P5: 상태 저장 + 진행률)

📚 실전 활용  (← 04)
 ├─ 폴더 구조 (트리 UI) + 폴더 지정 3요령
 ├─ 사례 16건 (카테고리 필터: 연구/MCP/지식베이스/SaaS/텔레그램/원격)
 ├─ 치트시트 (P6: 필터+검색+복사)
 └─ 비용 3습관 · 안전 3습관 (카드)
```

**내비게이션:** 데스크톱=고정 사이드바(섹션+스크롤스파이), 모바일=하단 탭바 또는 햄버거. 상단에 전역 검색.

---

## 5. 디자인 방향

* **전체 톤:** 절제된 모던 — 넉넉한 여백, 큰 타이포 위계, Bento 그리드 카드. 과한 글래스모피즘·과도한 그라디언트 금지.
* **폰트:** Pretendard (CDN). 코드·경로는 monospace.
* **색상 시스템 (P3의 핵심):**
  * **에르메스 (비서):** 앰버/오렌지 계열 — 배지 `에르메스 · 비서`
  * **프리덤 (작업대):** 블루 계열 — 배지 `프리덤 · 작업대`
  * **공통(전 기기):** 뉴트럴/그린 — 배지 `공통`
  * 경고(보안·과금)=레드, 팁=바이올렛. **이 배지를 표·카드·사례·치트시트 전체에 일관 적용.**
* **다크모드:** 토글 + `prefers-color-scheme` 존중. 모든 색을 CSS 변수로.
* **모션:** 스크롤 리빌(subtle fade-up), 아코디언 트랜지션, 탭 전환. `prefers-reduced-motion` 존중. 모션은 정보 전달 보조 수준으로만.
* **SVG 다이어그램 3종 (P2):**
  1. **레이어 구조** — 프리덤(넓은 판) 위에 에르메스(맥 위치에만 한 겹) 얹힌 그림
  2. **기기 배치도** — 기기 4개 노드 + 연결선(경로별 색·라벨: 위임/조작/화면제어) ← 가장 공들일 것
  3. **이어받기 흐름** — 본체 작업 → 폰 Orca 모바일 / 노트북 원격
  * SVG 내 텍스트도 다크모드 변수 사용. 모바일에서 세로 재배치 또는 가로 스크롤 허용.

---

## 6. 기술 사양

* **단일 `index.html`** — CSS·JS 인라인. 외부 의존은 Pretendard 폰트 CDN만 (실패 시 시스템 폰트 폴백).
* 프레임워크 없이 **바닐라 JS**. 상태는 `localStorage` (체크리스트 진행, 다크모드, 마지막 본 섹션). 접근 실패 대비 try/catch.
* 콘텐츠는 JS 데이터가 아니라 **HTML 마크업으로 직접 작성** (검색 엔진·Ctrl+F 호환, JS 꺼져도 읽힘). 필터·검색은 DOM 위에서 동작.
* 반응형: 모바일 우선. 표는 컨테이너 `overflow-x: auto`, 페이지 가로 스크롤 금지.
* 인쇄 스타일: 사이드바·버튼 숨김, 아코디언 전부 펼침.
* 접근성: 시맨틱 헤딩 위계(h1 1개), 배지에 색+텍스트 병기(색맹 대응), 키보드 포커스 스타일.

---

## 7. 콘텐츠 변환 규칙

1. **사실 변경 금지** — 모델명, 도구 수(15/10/18=43), 폴더 경로, 절차 순서를 원본 그대로.
2. **중복 표는 홈/이해 섹션에 1회** 배치하고, 다른 섹션에서는 요약 카드+앵커 링크로 참조.
3. **프롬프트 예시 블록**은 전부 `<pre>` + 복사 버튼. 한 글자도 요약하지 않는다 (사용자가 그대로 복사해 쓰는 용도).
4. **"✅ 완료 판정"**은 각 STEP 카드의 하단 고정 요소로 통일된 스타일.
5. **⚠️ 경고 3대장은 시각적으로 최상위 강조:** ① 게이트웨이 상시성(터미널 꺼도 텔레그램 동작) ② 백업에 설정 파일 제외 ③ 백업에 위키·Atlas 포함.
6. 이모지 남용 금지 — 배지·아이콘 시스템으로 대체하되 기기 이모지(🖥️💻📱)는 유지 가능.

---

## 8. 작업 순서 (Opus 5 실행 지시)

1. 이 계획서와 6개 md를 **전부 정독** (요약본 아닌 원문).
2. 색상 토큰·컴포넌트(배지, 카드, 아코디언, 복사 버튼, 체크리스트) 먼저 확정.
3. SVG 다이어그램 3종 작성 — **기기 배치도부터** (가장 복잡).
4. 섹션 순서대로 조립: 홈 → 이해 → 언제 뭘 쓰나 → 구축 → 실전.
5. 검색·필터·판단 도우미·localStorage 연결.
6. 검수: 모바일 뷰포트, 다크모드 양방향, 43개 수치·모델명 대조, 깨진 앵커, 콘솔 에러 0.
7. 산출: `/Users/bylee/Documents/시스템 구성 정리/index.html`

### 완료 기준 (체크리스트)
- [ ] 6개 문서의 모든 실질 내용이 누락 없이 포함 (사례 16건, STEP 전체, 점검 36항목, 치트시트 17행, 프롬프트 예시 전체)
- [ ] A/B/공통 배지가 전 섹션 일관 적용
- [ ] SVG 3종이 라이트/다크 모두 가독
- [ ] 체크리스트 상태가 새로고침 후 유지
- [ ] 프롬프트 복사 버튼 전수 동작
- [ ] 모바일(375px)에서 가로 스크롤 없음 (표 내부 제외)
- [ ] 파일 하나만 열면 오프라인에서도 동작 (폰트 제외)
